/**
 * AJAX-поиск для компонента smartconsultant.search
 * Используется, если JS вынесен отдельно от template.php
 */
(function() {
    document.addEventListener('DOMContentLoaded', function() {
        // Логика встроена в template.php для простоты
        // Этот файл — для случаев, когда JS нужно вынести отдельно
    });
})();
